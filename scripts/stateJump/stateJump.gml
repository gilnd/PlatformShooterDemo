/// stateJump()

// jump
jump = irandom_range(1.5, 2.5)
vsp = jump * -jumpspeed;
sprite_index = sprite_jump


/*if(not place_meeting(get_bbox_x()+1, bbox_bottom, oBlockScalable)) {
	// kepp going in that direction horrizonatly
	// after you are not colliding with the block
	hsp = (move*1.5) * movespeed;
}*/
	