/// @description Insert description here
// You can write your code in this editor
with(oGublin) {
	//behaviour_switch(bhGoAround, behaviour_timer == 0)
	//behaviour_switch(bhIdle)
	
	/*
	if distance_to_object(oPlayer) < range {
		behaviour_switch(bhFollowPlayer, behaviour_timer == 0)
	} else {
		behaviour_switch(bhGoAround, behaviour_timer == 0)
	}
	//*/
}