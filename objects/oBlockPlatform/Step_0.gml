/// @description Insert description here
// You can write your code in this editor
scrGetPlayerMoveInput();
if (instance_exists(oPlayer)){
    if (oPlayer.bbox_top >= bbox_bottom or oPlayer.bbox_bottom >= bbox_top or key_down > 0 ) // (oPlayer.y + oPlayer.sprite_height < y)
		mask_index = sEmpity;
    else mask_index = sprite_index; 
}

